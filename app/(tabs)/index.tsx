import { View, Text } from 'react-native';
import React from 'react';

const index = () => {
  return (
    <View className='flex flex-1 items-center justify-center'>
      <Text className='text-3xl text-red-600'>hello there</Text>
    </View>
  );
};

export default index;
